{
  "name": "newsletter-signup",
  "version": "1.1.0",
  "description": "This project is a simple newsletter signup page. It allows visitors to the website to enter their email address and subscribe to receive regular updates and news via email.",
  "main": "app.js",
  "scripts": {
    "start": "node src/server/app.js",
    "build": "rm -rf dist && mkdir dist && cp -r public src package.json dist/ && cd dist && npm install --only=production",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "author": "Guilherme Torres",
  "license": "ISC",
  "dependencies": {
    "@mailchimp/mailchimp_marketing": "^3.0.80",
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "request": "^2.88.2"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
